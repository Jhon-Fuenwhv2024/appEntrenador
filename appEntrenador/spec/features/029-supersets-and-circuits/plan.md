# Plan 029

1. Actualizar esquema de BD (`superset_order` o `group_id` en `routine_exercises`).
2. UI Entrenador: Drag & drop o botones para vincular ejercicios en superserie.
3. UI Cliente: Agrupar visualmente los ejercicios en el Workout Player.
4. Lógica del Player: Avanzar al siguiente ejercicio del circuito sin disparar descanso hasta terminar el grupo.