# 012 · Plan

1. DDL en `script_db.sql` + migración `004_workout_sessions.sql`.
2. Módulo `workout-sessions` (routes → controller → service).
3. API client + POST al terminar el player.
4. Historial en ficha trainer.
5. Actualizar docs y roadmap.
