# 010 · Plan

1. Crear `src/features/client/composables/useWorkoutSession.js`.
2. Estado mínimo + computed (`currentExercise`, progreso).
3. Acciones: `start`, `completeSet`, `skipRest`, cleanup timer.
4. Sin UI del player (consumo en 011).
