# Plan 027

1. Seleccionar e instalar librería de gráficos (ej. vue-chartjs).
2. Crear endpoint en el backend para agregar datos históricos de `workout_set_logs` y `body_composition_logs`.
3. Crear componente `ProgressChart` cargado de forma diferida (`defineAsyncComponent`).
4. Mostrar gráficas de peso corporal, IMC y evolución de ejercicios clave.