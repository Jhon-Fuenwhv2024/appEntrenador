# 011 · Plan

1. Enriquecer `listMyRoutines` con media del catálogo (match por nombre).
2. Ruta + `WorkoutPlayerView` + componente de media.
3. Cablear `useWorkoutSession` (010).
4. Botón Comenzar en `ClientDashboardView`.
