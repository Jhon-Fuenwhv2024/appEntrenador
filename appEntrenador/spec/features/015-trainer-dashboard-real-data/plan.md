# 015 · Plan

1. Extender `clients.service`: `getDashboardStats(trainerId)` + enriquecer listado con `routines_count` / `status`.
2. `GET /api/trainer/dashboard` + respuesta enriquecida en `GET /api/clients`.
3. Frontend: API client, props en `TrainerStatsSummary`, chart SVG desde serie, quitar mocks de status.
4. Docs `api.md` + tasks.
