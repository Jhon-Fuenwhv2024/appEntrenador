# Plan 030

1. Backend: Al guardar un `workout_set_log`, verificar si es un nuevo máximo (peso o volumen).
2. Retornar flag `is_pr` en la respuesta del guardado.
3. Frontend: Si hay un PR en la sesión, disparar animación al finalizar el workout.
4. Vista de Progreso: Resaltar los PRs históricos del cliente.