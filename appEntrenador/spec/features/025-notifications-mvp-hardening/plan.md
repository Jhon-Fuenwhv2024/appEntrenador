# Plan 025

1. Crear tabla `notifications` en `script_db.sql`.
2. Crear `modules/notifications` en el backend (rutas, controlador, servicio).
3. Crear composable `useNotifications` en el frontend.
4. Implementar `NotificationBadge` en el layout principal.
5. Generar notificaciones automáticamente al completar rutinas o asignarlas.