# 013 · Plan

1. Definir `on-primary` / `on-success` (`#0B0D12`) en `src/plugin/vuetify.js`.
2. Sustituir `color="#00E5FF"` por `color="primary"` en botones filled; retirar workarounds `text-black` redundantes.
3. Pass responsive: solo template/CSS/grid Vuetify en Login, dashboards, catálogo, rutinas cliente, player.
4. Documentar decisión de contraste en `docs/` y marcar `tasks.md`.
5. Validar con `npm run build` + smoke visual móvil.
